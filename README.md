# ollama_benchmark
A standalone Python benchmark designed to execute local Large Language Model (LLM) performance audits using the `ollama` daemon API.  This script runs an automated matrix of **10 distinct prompt dimensions** across an array of 8x open-weights models.
